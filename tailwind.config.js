/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./public/views/**/*.html"],
    theme: {
        extend: {
            animation: {
            },
            keyframes: {
            },
        },
    },
    plugins: [],
}
