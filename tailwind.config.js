/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./public/views/**/*.gohtml"],
    theme: {
        extend: {
            animation: {
            },
            keyframes: {
            },
        },
    },
    plugins: [],
}
